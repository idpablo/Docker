
#Pasear script shell, erro sintaxe " "
sed -i -e 's/\r$//' recreatlogstash.sh


elasticsearch

docker volume create --driver local --opt type=btrfs --opt device=/mnt/c/Docker/volumes/sda1 elasticsearch


docker volume create --driver local --opt type=tmpfs --opt device=/mnt/c/Docker/volumes/sda1 --opt o=size=10g,uid=1000 elasticsearch